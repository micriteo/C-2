<Window
    x:Class="SamuraiStandOff.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SamuraiStandOff"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Assets/backgroung.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20">
            <StackPanel.Resources>
                <FontFamily x:Key="CustomFont">/Assets/StayPixelRegular-EaOxl.ttf#Stay Pixel</FontFamily>
            </StackPanel.Resources>
            <Grid Margin="0,100,0,0">
                <!-- Black outline -->
                <TextBlock Text="Shogun StandOff" FontSize="90" Foreground="#000000" FontFamily="{StaticResource CustomFont}" Margin="2,2,0,0" />
                <TextBlock Text="Shogun StandOff" FontSize="90" Foreground="#000000" FontFamily="{StaticResource CustomFont}" Margin="-2,2,0,0" />
                <TextBlock Text="Shogun StandOff" FontSize="90" Foreground="#000000" FontFamily="{StaticResource CustomFont}" Margin="2,-2,0,0" />
                <TextBlock Text="Shogun StandOff" FontSize="90" Foreground="#000000" FontFamily="{StaticResource CustomFont}" Margin="-2,-2,0,0" />
                <!-- White text -->
                <TextBlock Text="Shogun StandOff" FontSize="90" Foreground="White" FontFamily="{StaticResource CustomFont}" />
            </Grid>
            <Button x:Name="myButton" 
                    Click="myButton_Click" 
                    Content="Start" 
                    Foreground="#FFFFFF"
                    FontSize="50" 
                    FontFamily="{StaticResource CustomFont}" 
                    Margin="-3,10,0,0"
                    BorderThickness="0"
                    Background="Transparent"/>
        </StackPanel>
    </Grid>
</Window>
